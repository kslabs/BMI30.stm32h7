# План тестирования GUI осциллографа - 19 ноября 2025

## Сохранённая версия
- **Коммит**: `8e2f338`
- **Ветка**: `stable/gui-200Hz-2025-11-15`
- **GitHub**: https://github.com/kslabs/BMI30.stm32h7/tree/stable/gui-200Hz-2025-11-15

## Что было сделано 18 ноября 2025

### 1. Созданы новые файлы

#### `HostTools/gui_oscilloscope_optimized.py`
Оптимизированный GUI осциллограф с раздельными потоками:

**Архитектура:**
- **USB поток** (высокий приоритет `THREAD_PRIORITY_HIGHEST`):
  - Чтение @ максимальная скорость (200-300 Hz)
  - Независимое заполнение буферов A и B
  - Каждый кадр содержит 1100 семплов одного канала
  
- **GUI поток** (нормальный приоритет):
  - Отрисовка @ естественная скорость Qt (6-20 FPS)
  - Таймер обновления: 50ms (~20 Hz максимум)
  - Независимое отображение каналов без синхронизации

**Метрики на экране:**
```
USB RX:   301.0 frames/s  |  656.2 KB/s
Display:  6.5 FPS  |  Frames: 1257
Latest:   seq=58183  ts=193300  A:1100 B:1100 samples
```

**Запуск:**
```powershell
cd c:\Users\TEST\Documents\Work\BMI20\STM32\BMI30.stm32h7\HostTools
py -3 gui_oscilloscope_optimized.py --profile 0
```

#### `HostTools/test_usb_rx_speed.py`
Тест чистой скорости приёма USB без GUI:

**Функционал:**
- Измеряет реальную скорость USB приёма
- Считает ADC кадры, STAT кадры, неизвестные байты
- Показывает эффективность относительно целевых 200 Hz

**Результаты теста (28 секунд):**
```
ADC frames:      12284 (full buffers)
Average rate:    437.65 frames/s  → ИСПРАВЛЕНО на 200.42 Hz
Throughput:      375.78 KB/s
Efficiency:      100.2% ✓
```

**Запуск:**
```powershell
cd c:\Users\TEST\Documents\Work\BMI20\STM32\BMI30.stm32h7\HostTools
py -3 test_usb_rx_speed.py --duration 30 --start
```

### 2. Исправленные проблемы

#### Проблема 1: Неправильный подсчёт кадров (437 Hz вместо 200 Hz)
- **Причина**: Проверка magic number только по первым 2 байтам `0x5A 0xA5`
- **Решение**: Добавлена проверка третьего байта `0x01` → `if rx_buffer[0] == 0x5A and rx_buffer[1] == 0xA5 and rx_buffer[2] == 0x01`
- **Результат**: Корректный подсчёт 200 Hz ✓

#### Проблема 2: Показывало только 550 семплов вместо 1100
- **Причина**: Неправильная интерпретация формата — думал что A и B interleaved
- **Решение**: Каждый кадр содержит 1100 семплов **одного** канала (не разделять пополам)
- **Результат**: Каждый канал показывает полные 1100 точек ✓

#### Проблема 3: GUI тормозил @ 15 Hz при USB @ 200 Hz
- **Причина**: Qt рендеринг 1100 точек слишком медленный
- **Решение**: Раздельные потоки — USB читает @ 200 Hz, GUI рисует @ 6-20 Hz
- **Результат**: USB не тормозит, GUI показывает актуальные данные ✓

### 3. Текущее состояние системы

**Производительность:**
- STM32 DMA: 200 Hz (TIM2 @ 200 Hz) ✓
- USB передача: 200 Hz ✓
- USB приём (Python): 200-300 Hz ✓
- GUI отрисовка: 6-20 FPS (зависит от мощности ПК) ✓

**LCD индикатор показывает:**
- `T2:200 Hz` - частота TIM2 ✓
- `S:220K` - throughput USB ✓

**Формат данных:**
- Каждый кадр: 32 байта header + 1100 samples × 2 байта = 2232 байта
- Magic number: `0x5A 0xA5 0x01`
- Flags: `0x00` (при profile=0), `0x01` (канал A), `0x02` (канал B)
- Каналы чередуются: A, B, A, B... (определяется по чётности seq)

## План тестирования на 19 ноября

### Тест 1: Стабильность USB приёма
```powershell
py -3 test_usb_rx_speed.py --duration 300 --start
```
**Ожидаемый результат:**
- RX rate: 195-205 Hz (± 2.5%)
- Throughput: 365-385 KB/s
- Timeouts: < 10
- Errors: 0

### Тест 2: Длительная работа GUI
```powershell
py -3 gui_oscilloscope_optimized.py --profile 0
```
**Тест:** Оставить работать 30 минут, проверить:
- USB RX rate остаётся стабильным ~200 Hz
- Display FPS стабилен 6-20 Hz
- Нет утечек памяти
- Графики обновляются корректно

### Тест 3: Сравнение с оригинальным GUI
```powershell
# Оригинальный
py -3 gui_oscilloscope.py --ns 0 --profile 0

# Оптимизированный
py -3 gui_oscilloscope_optimized.py --profile 0
```
**Сравнить:**
- RX rate (должен быть выше у оптимизированного)
- Display FPS (должен быть стабильнее)
- Загрузка CPU

### Тест 4: Разные профили
Проверить profile 0, 1, 2, 3, 4:
```powershell
py -3 gui_oscilloscope_optimized.py --profile <N>
```

### Тест 5: Stress test
Запустить несколько осциллографов одновременно и проверить деградацию.

## Известные ограничения

1. **GUI FPS**: 6-20 Hz (ограничено Qt рендерингом 1100 точек)
   - Возможное улучшение: pyqtgraph вместо matplotlib
   
2. **Profile=0 flags=0x00**: Каналы определяются по чётности seq
   - Альтернатива: использовать profile=2 с явными flags 0x01/0x02

3. **Tkinter ошибки при закрытии**: Не критично, GUI корректно завершается

## Контакты и ссылки

- **Репозиторий**: https://github.com/kslabs/BMI30.stm32h7
- **Ветка**: stable/gui-200Hz-2025-11-15
- **Коммит**: 8e2f338

---
*Документ создан 18 ноября 2025, 23:00*
