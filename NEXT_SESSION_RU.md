# Быстрый старт - Следующая сессия

## Текущий коммит
```bash
git log -1 --oneline
# 8ea87af Add quick start guide for next session
```

## Что работает ✅
- **Двухканальная передача**: Кадры A+B оба принимаются
- **Скорость**: ~50-90 пар/сек (ниже целевых 200 Гц)
- **Сборка**: text=126036 bss=160624 (VND_PAIR_BUFFERS=8)
- **Код профилирования**: Скомпилирован и интегрирован

## Что НЕ работает ❌
- **Вывод через CDC**: Статистика FPS не видна на COM4
- **Команда PERF**: Нет ответа на "PERF\n" через CDC

## Критическое исправление
**VND_PAIR_BUFFERS ДОЛЖЕН быть 8, НЕ 16**
- 16 → BSS=204KB → передача не работает (A=0 B=0)
- 8 → BSS=160KB → передача OK

## Быстрые команды тестирования

### 1. Базовый тест 10 секунд
```bash
py -3 HostTools\vendor_usb_start_and_read.py --window-sec 10 --pairs 10000 --ch-mode 2
```

### 2. Проверка состояния
```bash
py -3 HostTools\vendor_quick_status.py
```

### 3. USB reset (если устройство зависло)
```bash
py -3 -c "import usb.core; d=usb.core.find(idVendor=0xCAFE, idProduct=0x4001); d and d.reset()"
```

### 4. Сборка + прошивка
```bash
make -C Debug all
# Прошивка через OpenOCD task или вручную
```

## Приоритетные задачи на следующую сессию

### 1. Исправить вывод FPS через CDC (1-2 часа)

**Вариант A - Отладка через LED**:
```c
// В функции vnd_report_fps_stats() - строка 599
HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_13); // или Data_ready_GPIO22
```
Подтвердит, что функция вызывается, даже если CDC молчит.

**Вариант B - Опрос через VND Status** (рекомендуется):
```c
// Расширить ответ GET_STATUS, включив perf_stats
// Хост опрашивает через ctrl transfer вместо CDC
```

**Вариант C - Проверить CDC_Transmit_HS**:
```c
// В функции cdc_logf() - строка 345
uint8_t result = CDC_Transmit_HS((uint8_t*)cdc_evt_buf, (uint16_t)n);
if(result != USBD_OK) {
    // Зафиксировать ошибку через GPIO или vendor счётчик
}
```

### 2. Анализ узкого места (30 мин - после #1)
После получения статистики FPS определить проблему:
- **avg_prepare_us** > 2000 → Нужна оптимизация копирования ADC
- **avg_transmit_us** > 1000 → Проблема скорости USB
- **pair_interval_ms** > 10 → Проблема планировщика

### 3. Оптимизация на основе данных (2-3 часа)

**Если prepare медленный**:
- Профилировать копирование буфера ADC
- Использовать DMA или оптимизированный memcpy

**Если transmit медленный**:
- Проверить режим USB HS vs FS
- Верифицировать конфигурацию endpoint

**Если interval большой**:
- Пересмотреть логику планирования vnd_next_pair_ms
- Проверить управление буферами

## Файлы для проверки
1. `FPS_PROFILING_STATUS.md` - Подробный статус (англ.)
2. `USB_DEVICE/App/usb_vendor_app.c` - Строки 599-673 (функции FPS)
3. `USB_DEVICE/App/usbd_cdc_if.c` - Строки 286-290 (команда PERF)
4. `HostTools/vendor_usb_start_and_read.py` - Строка 39 (ch-mode=2)

## Ожидаемый график до 200 Гц
- Сессия 1 (сегодня): ✅ Инфраструктура профилирования FPS
- Сессия 2 (следующая): 🔧 Исправить вывод CDC + собрать данные
- Сессия 3: ⚡ Реализовать оптимизацию
- Сессия 4: 🎯 Достичь устойчивых 200 Гц

## Важные замечания
- Всегда делать USB reset после прошивки: `d.reset()`
- CDC на COM4 @ 115200 бод
- Устройство может требовать power cycle если совсем зависло
- Проверять размер `.elf` после изменений (должен быть ~126KB text)

## Что было сделано сегодня

### Изменения в коде
1. **USB_DEVICE/App/usb_vendor_app.c**:
   - Добавлены переменные измерения FPS (fps_pair_count, fps_frame_a/b_count)
   - Добавлена структура perf_stats_t для анализа времён
   - Добавлена функция get_us_approx() для микросекундного тайминга
   - Добавлена vnd_report_fps_stats() - автоотчёт каждые 2с
   - Добавлена vnd_print_perf_stats() - ручная команда PERF
   - ИСПРАВЛЕНО VND_PAIR_BUFFERS=8 (16 вызывало переполнение BSS)
   - Интегрирован тайминг в vnd_prepare_pair() и TxCplt

2. **USB_DEVICE/App/usbd_cdc_if.c**:
   - Добавлен обработчик команды PERF для вызова vnd_print_perf_stats()

3. **HostTools/vendor_usb_start_and_read.py**:
   - Исправлен ch-mode по умолчанию с 0 (только A) на 2 (двухканальный)

### Найденные проблемы
1. **VND_PAIR_BUFFERS=16 ломает передачу**
   - Симптом: Устройство получает START, но не отправляет кадры (A=0 B=0)
   - Причина: BSS увеличился до 204KB → повреждение памяти
   - Решение: Возврат к VND_PAIR_BUFFERS=8
   - Результат: Передача восстановлена (48-95 пар/сек)

2. **Отчёты FPS через CDC не видны**
   - Симптом: Нет вывода на COM4 несмотря на вызов vnd_report_fps_stats()
   - Возможные причины:
     * CDC TX буфер заполнен/заблокирован во время vendor streaming
     * CDC_Transmit_HS() падает молча (неблокирующий вызов)
     * Конфликт cdc_logf() с vendor USB активностью
   - Попробованные обходы:
     * ✗ Команда "PERF" через CDC - нет ответа
     * ✗ STOP + PERF - нет ответа
   - Ещё не пробовали:
     * Проверить return code CDC_Transmit_HS
     * Добавить GPIO LED toggle для подтверждения выполнения
     * Использовать VND bulk IN для статистики вместо CDC

3. **Низкая пропускная способность (~50-90 Гц вместо 200 Гц)**
   - Наблюдается: HOST_SUM показывает 33-88 пар/сек
   - Ожидается: 200 пар/сек (период 5мс @ 200Гц)
   - Следующие шаги: Анализ данных профилирования для поиска узкого места

### Результаты тестов
```
Тест 1 (после исправления VND_PAIR_BUFFERS=8):
[HOST_SUM] +1.0s A=48 (46.7/s) B=49 (47.6/s) STAT=0 timeouts=0 pipes=0
[HOST_SUM] +1.1s A=36 (33.4/s) B=36 (33.4/s) STAT=0 timeouts=0 pipes=0
[HOST_SUM] +1.1s A=95 (88.3/s) B=95 (88.3/s) STAT=0 timeouts=0 pipes=0
[HOST_SUM] +1.0s A=62 (61.9/s) B=62 (61.9/s) STAT=0 timeouts=0 pipes=0
```
**Анализ**: Двухканальная передача работает, но скорость нестабильная (33-88 Гц)

---
**Последнее обновление**: 2025-11-03  
**Коммит**: 8ea87af  
**Статус**: Профилирование FPS скомпилировано, двухканальная передача работает, вывод CDC ожидает исправления
