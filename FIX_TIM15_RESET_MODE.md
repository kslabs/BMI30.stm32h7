# Исправление синхронизации ADC с DMA @ 400 Гц

**Дата:** 17 декабря 2025
**Проблема:** ADC не получает данные (все нули), TIM2 CH2 показывает 400 Гц вместо ожидаемых 200 Гц

## 🔍 Диагностика

### Измерения осциллографа
- **TIM2 CH2**: 400 Гц (ожидалось 200 Гц)
- **Выход:** Все нули в GUI вместо реальных данных АЦП

### Анализ архитектуры
**Старая конфигурация (НЕПРАВИЛЬНАЯ):**
```
TIM2 @ 200 Hz (базовый) → TRGO=OC1REF (CH1 HIGH на 4.8ms из 5ms)
  ↓
TIM15 @ 275 kHz (GATED mode) → работает только пока CH1=HIGH
  ↓ TRGO=UPDATE
ADC1/ADC2 (External Trigger) → заполняют буферы
  ↓ DMA
USB @ 400 Hz (чередование A/B буферов)
```

**Проблемы:**
1. **TIM15 GATED** от CH1 даёт одно длинное окно 4.8 мс @ 200 Гц
2. **Нет синхронизации** с чередованием буферов A/B @ 400 Гц
3. ADC **не сбрасывается** между буферами → накопление ошибок

## ✅ Решение

### Новая архитектура (ПРАВИЛЬНАЯ)
```
TIM2 @ 200 Hz (базовый)
  CH3: Меандр 50% (CCR3 = ARR/2 = 2500)
    ↓ Фронты на 0, 2.5, 5.0, 7.5... мс = 400 Hz
  TRGO = OC3REF → Импульсы @ 400 Hz
    ↓
TIM15 @ 275 kHz (RESET mode) → сбрасывается каждые 2.5 мс
  CNT сброшен в 0 → начинает новый цикл
    ↓ TRGO=UPDATE @ 275 Hz
ADC1/ADC2 (External Trigger) → синхронно запускаются
  ↓ DMA ONESHOT
USB @ 400 Hz (чередование A/B буферов) → СИНХРОНИЗИРОВАНО
```

### Преимущества
1. ✅ **TIM15 сбрасывается каждый период** DMA (2.5 мс)
2. ✅ **ADC начинает с CNT=0** для каждого буфера A/B
3. ✅ **Точная синхронизация** с USB @ 400 Гц
4. ✅ **CH2 теперь правильно показывает 400 Гц** (не артефакт)

## 📝 Изменения в коде

### 1. TIM2: TRGO изменён с OC1REF на OC3REF
**Файл:** `Core/Src/main.c:1754`

```c
// БЫЛО (НЕПРАВИЛЬНО):
sMasterConfig.MasterOutputTrigger = TIM_TRGO_OC1REF;  // 1 раз за период @ 200 Hz

// СТАЛО (ПРАВИЛЬНО):
sMasterConfig.MasterOutputTrigger = TIM_TRGO_OC3REF;  // Меандр → 2 фронта @ 400 Hz
```

### 2. TIM15: SlaveMode изменён с GATED на RESET
**Файл:** `Core/Src/main.c:1943`

```c
// БЫЛО (НЕПРАВИЛЬНО):
sSlaveConfig.SlaveMode = TIM_SLAVEMODE_GATED;  // Работает пока CH1=HIGH

// СТАЛО (ПРАВИЛЬНО):
sSlaveConfig.SlaveMode = TIM_SLAVEMODE_RESET;  // Сброс на каждом фронте TRGO @ 400 Hz
```

### 3. Диагностика ADC/DMA добавлена
**Файл:** `Core/Src/adc_stream.c:469-471`

```c
printf("[ADC][DIAG] Before DMA start: ADC1->CR=0x%08lX DMA1_Stream0->CR=0x%08lX NDTR=%lu\r\n",
       (unsigned long)ADC1->CR, (unsigned long)DMA1_Stream0->CR, (unsigned long)DMA1_Stream0->NDTR);
```

## 🧪 Тестирование

### Ожидаемое поведение после прошивки
1. **TIM2 CH2**: 400 Гц (фронты меандра CH3)
2. **TIM15**: Счётчик CNT сбрасывается каждые 2.5 мс
3. **ADC**: Заполняет буферы синхронно с DMA @ 400 Гц
4. **GUI**: Показывает 4 канала (A_even, A_odd, B_even, B_odd) с реальными данными АЦП

### Команды проверки
```bash
# 1. Мониторинг логов ADC через COM4
py -3 monitor_adc.py

# 2. Запуск GUI осциллографа
py -3 HostTools/gui_oscilloscope_optimized.py --profile 0

# 3. Проверка сырых USB кадров
py -3 HostTools/dump_raw_frames.py
```

### Ключевые логи для поиска
- `[ADC][CALIB]` — калибровка ADC OK/FAILED
- `[ADC][DIAG] Before DMA start` — состояние регистров перед стартом
- `[TIM15] Pre-Start` — конфигурация TIM15
- `[TIM2] Started` — TIM2 запущен с ARR/CCR

## 📊 Временная диаграмма

```
TIM2 (200 Hz базовый):
  Period = 5000 µs
  CH1: ▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████
       |                      Pulse=4800µs (96%)           |
       0                                                5000 µs

  CH3: ████████████████████████████▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████...
       |         CCR3=2500µs        |       2500µs       |
       ↑ Front @ 0                  ↑ Front @ 2500       ↑ Front @ 5000
       = 400 Hz (период 2500 µs)

TIM15 RESET mode:
  CNT: 0→999→0→999→0→999→0→999→...
       ↑     ↑     ↑     ↑
       0     2.5   5.0   7.5 ms (сброс на каждом фронте CH3)

ADC DMA:
  Буфер A: [0-2.5ms]   ← TIM15 сброшен @ 0 ms
  Буфер B: [2.5-5ms]   ← TIM15 сброшен @ 2.5 ms
  Буфер A: [5-7.5ms]   ← TIM15 сброшен @ 5 ms
  ...
```

## 🔧 Дополнительные диагностические изменения

### TEST_OVERLAY_SAWTOOTH отключён
**Файл:** `USB_DEVICE/App/usb_vendor_app.c:203`
```c
#define TEST_OVERLAY_SAWTOOTH 0  // DISABLED: Testing real ADC data
```

Это позволяет видеть **реальные данные АЦП** вместо тестового пилообразного сигнала.

## 📌 Следующие шаги

1. ✅ Прошить новую прошивку
2. ⏳ Запустить `monitor_adc.py` для чтения логов
3. ⏳ Проверить GUI — должны появиться реальные данные АЦП
4. ⏳ Измерить осциллографом TIM2 CH2 — теперь корректно 400 Гц
5. ⏳ Проверить TIM15 — CNT должен сбрасываться каждые 2.5 мс

## 🎯 Успех означает

- GUI показывает 4 канала с **реальными волнами** (не нули)
- USB RX rate ≈ **400 frames/s** (200 Hz × 2 канала)
- Нет пропусков кадров (gaps = 0)
- Логи показывают `[ADC][CALIB] OK` для обоих ADC
