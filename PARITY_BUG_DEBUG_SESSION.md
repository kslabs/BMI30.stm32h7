# Отладка Parity Transmission Bug - 16 дек 2025

## ПРОБЛЕМА
400Hz even/odd архитектура реализована, но **parity ВСЕГДА = 0 (even)** вместо чередования 0/1.

## НАБЛЮДЕНИЯ
1. **GUI получает данные**: 85 fps, кадры A и B приходят
2. **frame_seq инкрементируется правильно**: 1505→1506→1507... (включая НЕЧЁТНЫЕ)
3. **НО parity=0 для ВСЕХ frame_seq** (даже для 1505, 1507 которые нечётные!)
4. **flags НИКОГДА не имеет бита 0x80** (всегда 0x05/0x06, без parity бита)
5. **reserved2=0 всегда** (дублирует parity=0)

## КОД РАСЧЁТА (в usb_cdc_proto.c)
```c
uint8_t parity = (uint8_t)(s_frame_seq & 0x01u);  // Должен давать 0 для чётных, 1 для нечётных

if (parity) {
    hdr.flags |= 0x80;  // Установить бит 7 для нечётных кадров
}
hdr.reserved2 = (uint16_t)parity;  // Дублирование для отладки
```

## ГИПОТЕЗЫ
1. **САМАЯ ВЕРОЯТНАЯ**: `s_frame_seq` инициализирован как ЧЁТНОЕ и инкрементируется +2 (а не +1)
   - Нужно проверить: где `s_frame_seq` инициализируется
   - Проверить: сколько раз вызывается `s_frame_seq++` на каждую пару A+B

2. Компилятор оптимизировал `parity = seq & 1` в константу 0 (маловероятно для -O0)

3. `s_frame_seq` не то значение (но reserved=frame_seq показывает правильные 1505,1506...)

4. Передача parity работает, но GUI парсит неправильно (НО reserved2=0 подтверждает parity=0!)

## СЛЕДУЮЩИЕ ШАГИ
1. ✅ Убрать вызов `adc_get_buffer_parity()`, использовать прямой расчёт - **СДЕЛАНО**
2. ✅ Инкрементировать `s_frame_seq++` после КАЖДОЙ отправки (A и B отдельно) - **СДЕЛАНО**
3. **TODO**: Проверить ИНИЦИАЛИЗАЦИЮ `s_frame_seq` - может быть чётное начальное значение?
4. **TODO**: Добавить `volatile` к `s_frame_seq` чтобы избежать оптимизации
5. **TODO**: Вывести printf ПРЯМО в момент расчёта parity (перед if)
6. **TODO**: Проверить assembly код для `parity = seq & 1`

## КОД ДЛЯ ПРОВЕРКИ
В `usb_cdc_proto.c` строка ~118:
```c
// ДОБАВИТЬ DEBUG ПЕРЕД if (parity):
printf("[PAR] seq=%lu parity_calc=%u\r\n", s_frame_seq, parity);
```

## СТАТУС СБОРКИ
- Последняя успешная сборка: 150920 bytes (150 KB)
- Прошивка: работает, данные приходят
- GUI: показывает frame_seq правильно, но parity=0 всегда

## КРИТИЧЕСКОЕ ОТКРЫТИЕ
**frame_seq=1505** (нечётный!) но **parity=0** → расчёт `1505 & 1` должен давать 1, но даёт 0!

Либо:
- `s_frame_seq` не 1505 в момент расчёта
- Или `&` оператор не работает (невозможно!)
- Или parity вычисляется ПЕРЕД инкрементом, а reserved заполняется ПОСЛЕ

## РЕКОМЕНДАЦИЯ
**СРАВНИТЬ МОМЕНТ ЧТЕНИЯ s_frame_seq**:
- Для parity: читается в строке 119
- Для reserved: записывается в строке 132
- Между ними может быть ИЗМЕНЕНИЕ s_frame_seq!

**РЕШЕНИЕ**: Сохранить локальную копию:
```c
uint32_t current_seq = s_frame_seq;  // Зафиксировать ДО любых расчётов
uint8_t parity = (uint8_t)(current_seq & 0x01u);
// ...
hdr.reserved = (uint16_t)(current_seq & 0xFFFFu);
hdr.reserved2 = (uint16_t)parity;
```

ПРОДОЛЖИТЬ В СЛЕДУЮЩЕЙ СЕССИИ!
