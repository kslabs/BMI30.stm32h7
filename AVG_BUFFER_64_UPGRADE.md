# Расширение буферизации с усреднением до 64 буферов

## Обзор изменений

Реализована поддержка усреднения до **64 буферов** (вместо прежних 32) с **шагом 8** для оптимального баланса между гибкостью и простотой выбора в GUI.

## Доступные значения avg_n

**Новый диапазон:** `8, 16, 24, 32, 40, 48, 56, 64`

**Рекомендуемые значения для типичного использования:** `32-48` буферов

**Default:** `20` (сохранён для обратной совместимости с существующими конфигурациями)

## Технические детали

### Использование памяти

**Размер буфера `vnd_avg_sum`:**
```c
static uint32_t vnd_avg_sum[2][2][MAX_FRAME_SAMPLES]; /* [parity][ch][i] */
```

- **Структура:**
  - `[2]` - чётность (even/odd)
  - `[2]` - каналы (A и B)
  - `[MAX_FRAME_SAMPLES]` = 1360 семплов

- **Размер:** `2 × 2 × 1360 × 4 байта = 43,520 байт (~42.5 КБ)`
- **Важно:** Размер **НЕ зависит** от `VND_AVG_MAX_N` и остался прежним

### Безопасность переполнения uint32_t

**Текущий тип:** `uint32_t` (максимум: 4,294,967,295)

**Максимальное значение ADC:** `65,535` (uint16_t)

**Проверка при 64 буферах:**
- **Вычисление:** `65,535 × 64 = 4,194,240`
- **Процент использования:** `4,194,240 / 4,294,967,295 ≈ 97.7%`
- **Запас:** ~2.3% до переполнения ✓ **ДОСТАТОЧНО**

**Для справки:**
- При 32 буферах: `65,535 × 32 = 2,097,120` (~48.8% использования)
- При 128 буферах: `65,535 × 128 = 8,388,480` ⚠️ **ПЕРЕПОЛНЕНИЕ** (требуется uint64_t)

### Производительность

**CPU нагрузка:**
- Количество операций сложения **не изменилось** (по-прежнему `+=` для каждого входного семпла)
- Деление будет на большее число (64 вместо 32), но это **1 операция на семпл**
- DSP обработка выполняется **после** усреднения и не зависит от `avg_n`

**Вывод:** Незначительное влияние на производительность CPU

## Изменения в коде

### Firmware (STM32)

**Файл:** `USB_DEVICE/App/usb_vendor_app.c`

```c
// Было:
#define VND_AVG_MAX_N  32u

// Стало:
#define VND_AVG_MAX_N  64u  /* Максимум 64 буфера для усреднения (шаг 8: 8/16/24/32/40/48/56/64) */
```

```c
// Было:
static volatile uint8_t vnd_avg_n = 20; /* 1..32 */

// Стало:
static volatile uint8_t vnd_avg_n = 20; /* 1..64 (рекомендуемые значения: 8/16/24/32/40/48/56/64) */
```

### Python GUI

**Файл:** `HostTools/BMI30.200.py`

```python
# Было:
avg_items = ["4","8","12","16","20","24","28","32"]

# Стало (шаг 8 для оптимального баланса):
avg_items = ["8","16","24","32","40","48","56","64"]
```

**Комментарий в коде:**
```python
# Шаг 8 для диапазона 8..64 (оптимальный баланс: достаточно вариантов, но не перегружаем UI)
```

## Использование

### Выбор значения через GUI

1. Запустите `BMI30.200.py`
2. В верхней панели найдите выпадающий список выбора avg_n
3. Выберите значение из диапазона: `8, 16, 24, 32, 40, 48, 56, 64`
4. Переключение на режим AVG_ROI автоматически применит выбранное значение

### Программное управление (Python)

```python
from usb_vendor.usb_stream import USBStream, CMD_SET_STREAM_MODE

stream = USBStream()

# Установить avg_n=48 и переключиться в режим AVG_ROI
avg_n = 48
payload = bytes([2, avg_n])  # mode=2 (AVG_ROI), avg_n=48
stream.send_cmd(CMD_SET_STREAM_MODE, payload)
```

### Команда по USB

**Формат:** `CMD_SET_STREAM_MODE` (0x1A)

**Payload:**
- Байт 0: `mode` (2 = AVG_ROI)
- Байт 1: `avg_n` (1..64, рекомендуемые: 8/16/24/32/40/48/56/64)

**Пример:**
```
0x1A 0x02 0x30  // mode=2 (AVG_ROI), avg_n=48
```

## Практические рекомендации

### Выбор значения avg_n

**Низкий шум, высокая скорость:** `8-16` буферов
- Минимальная задержка (~40-80 мс при 200 Гц частоте буферов)
- Умеренное подавление шума

**Сбалансированный режим:** `24-32` буфера
- Оптимальное соотношение задержка/качество
- Хорошее подавление шума
- Задержка ~120-160 мс

**Высокое качество, низкий шум:** `40-56` буферов
- Сильное подавление шума
- Задержка ~200-280 мс
- Подходит для стационарных измерений

**Максимальное подавление шума:** `64` буфера
- Предельное подавление высокочастотных шумов
- Задержка ~320 мс
- Для очень медленных сигналов

### Оценка задержки

**Формула:**
```
Delay (ms) = avg_n × (1000 / buf_rate_hz)
```

**Пример для buf_rate = 200 Гц:**
- avg_n=8:  40 мс
- avg_n=16: 80 мс
- avg_n=24: 120 мс
- avg_n=32: 160 мс
- avg_n=48: 240 мс
- avg_n=64: 320 мс

**Пример для buf_rate = 300 Гц:**
- avg_n=8:  ~27 мс
- avg_n=16: ~53 мс
- avg_n=24: ~80 мс
- avg_n=32: ~107 мс
- avg_n=48: ~160 мс
- avg_n=64: ~213 мс

## Обратная совместимость

- **Default значение** `avg_n = 20` сохранено
- Старые конфигурации с `avg_n` в диапазоне 1-32 будут работать корректно
- Значение 20 отсутствует в новом списке GUI, но можно установить программно
- При значении вне нового шага (например, 12 или 20) GUI автоматически добавит его в список

## Ограничения

**❌ 128 буферов НЕ планируется**
- Потребует изменения типа `vnd_avg_sum` с `uint32_t` на `uint64_t`
- Удвоит потребление памяти до ~85 КБ
- Задержка составит ~640 мс при 200 Гц (слишком большая для практического применения)

**✓ Текущий диапазон 8-64 покрывает все практические сценарии**

## Тестирование

1. **Сборка firmware:**
   ```powershell
   powershell -ExecutionPolicy Bypass -File .vscode/build-and-flash.ps1
   ```

2. **Запуск GUI:**
   ```bash
   python HostTools/BMI30.200.py
   ```

3. **Проверка значений:**
   - Откройте выпадающий список avg_n в верхней панели
   - Убедитесь, что доступны значения: 8, 16, 24, 32, 40, 48, 56, 64
   - Переключитесь на режим AVG_ROI (кнопка 6 или режим диагностики)
   - Выберите разные значения и проверьте работу

4. **Проверка через USB статус:**
   ```python
   from usb_vendor.usb_stream import USBStream, CMD_GET_STATUS
   
   stream = USBStream()
   # Отправить GET_STATUS и проверить текущее avg_n в ответе
   ```

## История изменений

**Дата:** 14 января 2026 г.

**Commit:** `feat: расширение буферизации с усреднением до 64 буферов (шаг 8)`

**Автор:** BMI30 Development Team

**Предыдущее состояние:**
- Максимум: 32 буфера
- Шаг: 4 (4/8/12/16/20/24/28/32)
- Список в GUI: 15 значений

**Новое состояние:**
- Максимум: 64 буфера
- Шаг: 8 (8/16/24/32/40/48/56/64)
- Список в GUI: 8 значений (оптимальный баланс)

## См. также

- [DC_ADAPT_COMMAND.md](DC_ADAPT_COMMAND.md) - Управление DC-адаптацией
- [HOST_SETUP_RU.md](HostTools/HOST_SETUP_RU.md) - Настройка хостового ПО
- [USBprotocol.txt](USBprotocol.txt) - Протокол USB Vendor команд
- [RELEASE_NOTES.md](RELEASE_NOTES.md) - История изменений прошивки
